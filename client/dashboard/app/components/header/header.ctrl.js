define([
    'headerSrv'
    
], function(headerSrv) {
    'use strict';
    function headerCtrl(Restangular, $location, $window) {

        var vm = this;
    }

    headerCtrl.$inject = ['Restangular',  '$location','$window'];

    return headerCtrl;
    
});