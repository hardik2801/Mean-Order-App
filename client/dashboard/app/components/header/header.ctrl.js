define([
    'headerSrv'
    
], function(headerSrv) {
    'use strict';
    function headerCtrl(Restangular, $location, $window) {

        var vm = this;

        // var serviceHelper = headerSrv(Restangular);

        
    }

    headerCtrl.$inject = ['Restangular',  '$location','$window'];

    return headerCtrl;
    
});